defmodule Mix.Tasks.Phx.Gen.Auth.Passwordless do
  @shortdoc "Generates passwordless authentication logic for a resource"

  use Mix.Task

  def run(args) do
    IO.puts("Hello from Mix.Tasks.Phx.Gen.Auth.Passwordless")
  end
end
